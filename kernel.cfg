{
  version: 1,
  machine: "riscv64",
  memory_size: 128,
  bios:    "bbl64.bin",           // あるいは riscv-pk 経由の BIOS
  kernel:  "kernel.elf",          // ELF カーネルを直接読み込むなら
  cmdline: "console=hvc0 root=/dev/vda rw",
  drive0: { file: "fs.img" },     // ← ブロックデバイスとして fs.img をマウント
}

